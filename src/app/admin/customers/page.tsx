import CustomersListView from 'src/sections/admin/customers/customers-list-view';

export const metadata = {
  title: 'Клієнти | Admin',
};

export default function UserListPage() {
  return <CustomersListView />;
}
